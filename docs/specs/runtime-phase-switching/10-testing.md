---
状態: 確定
最終更新: 2026-09-01
---

# テスト項目

**方針は [07-testing.md](../07-testing.md) が正本。** ここには段3・段4 で追加する項目だけを書く。
チェックボックスを埋めながら実装する。

## 1. データ取得（段3）

- [ ] T-1 `READONLY_PROXY_URL` が空のとき、**取得を試みずに** `EventSnapshot.unavailable()` を返す
- [ ] T-2 プロキシが 500 を返しても**例外を投げず**、前回のキャッシュが保持される
- [ ] T-3 プロキシがタイムアウトしても同上
- [ ] T-4 応答に `rows` が無い／JSON でない場合も同上
- [ ] T-5 例外メッセージに **SQL 本文と鍵が含まれない**（テーブル名だけが載る）
- [ ] T-6 組み立てた SQL が `SELECT` で始まらなければ**送信前に**拒否される
- [ ] T-7 `LIVE_TABLES` に無いテーブル名を要求すると拒否される
- [ ] T-8 `users` の要求列に `email` / `password_hash` が**構造的に入りえない**
- [ ] T-9 バックグラウンドタスクは**起動直後に1回**走り、以後 `SNAPSHOT_TTL_SEC` ごとに回る
- [ ] T-10 取得中でも `POST /recommend/cells` が `COVERAGE` で 200 を返す

## 2. 決定表の組み立て

- [ ] T-11 **評価が無い訪問が行にならない**（未評価を評価0として扱わない）
- [ ] T-12 `decision_table_size` が「評価済みチェックイン件数」と一致する
- [ ] T-13 `role <> 'participant'` の行が除外される
- [ ] T-14 `is_active = 0` のブースの行が除外される
- [ ] T-15 **他イベントの行が1件も混ざらない**（2イベント分を入れて確認する）
- [ ] T-16 同一参加者×同一ブースの重複が畳まれる
- [ ] T-17 条件属性が `ENABLED_ATTRIBUTES` の2個だけで構成される
- [ ] T-18 決定表 → `generate_rules` が既存の `drsa/` テストと矛盾しない結果を返す

## 3. フェーズ切り替え（段4）— **本仕様の核心**

- [ ] T-19 決定表 0 件 → `phase = COVERAGE`
- [ ] T-20 決定表 `PHASE_SIMILARITY_MIN - 1` 件 → `COVERAGE`
- [ ] T-21 決定表 `PHASE_SIMILARITY_MIN` 件ちょうど → `SIMILARITY`
- [ ] T-22 決定表 `PHASE_DRSA_MIN` 件以上 かつ 品質ゲート通過 → `DRSA`
- [ ] T-23 決定表 `PHASE_DRSA_MIN` 件以上 だが ゲート不通過 → `SIMILARITY`
- [ ] T-24 **件数が増えるにつれて `COVERAGE → SIMILARITY → DRSA` と実際に切り替わる**
      （1つのテストで決定表を育てながら連続して確認する。**これが「当日切り替わる」ことの証明**）
- [ ] T-25 決定表が縮んだ場合（起きないはずだが）にも例外を投げない

## 4. 退避

- [ ] T-26 規則キャッシュが空 → 判定 DRSA でも `phase = SIMILARITY`
- [ ] T-27 スナップショット未構築 → 判定 SIMILARITY でも `phase = COVERAGE`
- [ ] T-28 事前アンケート未回答の参加者 → `SIMILARITY` を要求されても `COVERAGE` へ退避
- [ ] T-29 近傍が1人も作れない → `COVERAGE` へ退避
- [ ] T-30 戦略の実行中に例外 → 落ちずに1段下へ退避し、200 を返す
- [ ] T-31 実行が `RESPONSE_BUDGET_MS` を超える → 打ち切って `COVERAGE`、200 を返す
- [ ] T-32 **退避したとき `phase` に実際の戦略が入る**（判定結果が入っていないこと）
- [ ] T-33 退避の理由が JSONL ログに出る

## 5. 戦略の中身

- [ ] T-34 `SIMILARITY`: 近傍1人が高評価しただけのブースが最上位に来ない（**ベイズ縮約の確認**）
- [ ] T-35 `SIMILARITY`: `gender` が近傍の距離に影響しない
- [ ] T-36 `SIMILARITY`: `visitor_count` を変えてもスコア順位が変わらない（**人気度が混ざらない**）
- [ ] T-37 `DRSA`: 適合規則0本の候補が `score = 0.5` 付近に落ち、順序が付く
- [ ] T-38 `DRSA`: `reason.rules` に規則本体が入らない（id と要約のみ）
- [ ] T-39 全戦略: **全候補にスコアが付く**（S-1）
- [ ] T-40 全戦略: `interest_match = MISMATCH` の候補がスコア0にならない（P-5。セレンディピティが観測不能になる）

## 6. 「起きてはいけないこと」（07-testing.md の不変条件を段4でも確認する）

- [ ] T-41 **推薦結果が人気度ランキングに退化しない。** `visitor_count` と最終順位の相関を測り、
      COVERAGE / SIMILARITY / DRSA の**全フェーズで**しきい値以内であること
- [ ] T-42 どんな入力でも 500 を返さない（壊れた JSON・巨大な候補数・空の候補）
- [ ] T-43 同一入力で同一出力（乱数を使う箇所はシードが固定されている）

## 7. 観測

- [ ] T-44 `/ops/state` の `phase.current` が、実際に返した `phase` と一致する
- [ ] T-45 `/ops/state` の `notes` に「未結線」の記述が残っていない
- [ ] T-46 `/ops/rebuild` が実際に1周させ、`decision_table_size` が更新される
- [ ] T-47 `/ready` がスナップショットの状態を正しく表す（**プローブには使わない**ことをコメントで残す）
- [ ] T-48 フェーズが変わった回のログに `phase_changed` が出る

## 8. 統合（実データに近い形で）

- [ ] T-49 合成シナリオを時系列に流し、**フェーズが3段階とも実際に発火する**ことを確認する
      （`tools/build_report.py` の拡張でよい）
- [ ] T-50 さくらプロキシのモック（1リクエスト1SQL・エラーは500に潰れる）に対して段3が動く
- [ ] T-51 本番相当の決定表サイズ（数百行）で、1周の所要時間が `SNAPSHOT_TTL_SEC` を超えない

## 事前検証（本番前に必ず1回）

- [ ] V-1 **デプロイ後、`/ops/state` が `snapshot.ok = true` を返す**（鍵と URL が通っている）
- [ ] V-2 リハーサルイベントで評価を30件入れ、**`SIMILARITY` に上がることを目視で確認する**
- [ ] V-3 その状態で解放を起こし、`card_unlock_events.phase` に `SIMILARITY` が記録される
- [ ] V-4 プロキシを止めても解放が成功し、`FALLBACK_COVERAGE` になる
